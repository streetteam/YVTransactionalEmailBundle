<h1>TransactionalEmail creation</h1>

<form action="{{ path('yv_transactional_email_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('yv_transactional_email_index') }}">
            Back to the list
        </a>
    </li>
</ul>
